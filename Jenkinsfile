pipeline {
    agent any

    stages {
      stage('git checkout') {
        steps{
          echo 'git branch: 'main', url: 'https://github.com/venkyy8/monday-practice.git'
        }
      }
    }
}
